<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns="http://www.w3.org/ns/widgets"
        xmlns:tizen="http://tizen.org/ns/widgets"
        id="http://chrisatdigi.com/tooty"
        version="1.0.0"
        viewmodes="maximized">

  <tizen:application id="TOOTY00001.Tooty" package="TOOTY00001" required_version="2.3" type="webapp"/>

  <name>Tooty</name>
  <icon src="icon.png"/>
  <content src="index.html"/>
  <tizen:profile name="tv"/>

  <!-- Filesystem access for HDD/USB scanning -->
  <tizen:privilege name="http://tizen.org/privilege/filesystem.read"/>

  <!-- localStorage is sufficient for metadata cache — no additional privilege needed -->

  <!-- Network access for TMDB API calls -->
  <tizen:privilege name="http://tizen.org/privilege/internet"/>
  <access origin="https://api.themoviedb.org" subdomains="true"/>
  <access origin="https://image.tmdb.org" subdomains="true"/>

  <!-- AVPlay for video playback -->
  <tizen:privilege name="http://tizen.org/privilege/tv.avplay"/>

  <tizen:setting screen-orientation="landscape"
                 context-menu="disable"
                 background-support="disable"
                 encryption="disable"
                 install-location="auto"
                 hwkey-event="enable"/>
</widget>
